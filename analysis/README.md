# Analysis

This directory is the report-facing home for analysis outputs.

Recommended outputs:

- `results/raw/` for `.jtl`, CSV, and other raw measurement files
- `results/figures/` for PNG figures used in the report
- `results/tables/` for summary CSVs and comparison tables

The runnable analysis code lives in `tools/`.
